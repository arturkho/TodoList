function removeTask(taskId, tasks, tableId) {
    // localStorage.removeItem(tableId);
    // let taskTableLists = document.getElementsByTagName('ul');
    // tasks.splice(taskId, 1);
    // for(let i = 0; i < taskTableLists.length; i++){
    //     taskTableLists[i].removeChild(document.querySelector(`#row${taskId}`));
    // }
    //
    // localStorage.setItem(tableId, JSON.stringify(tasks));
}